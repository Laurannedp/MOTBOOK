<div id=“tasks”>
  <% if mot.tasks.any? %>
  <% mot.tasks.each do |task| %>
  <div class="container border mt-2" id="wrapperedittask">
    <div class="row">
      <div class="col-4 pt-4" >
        <%= task.name %>
      </div>
      <div class="col-2 pt-4">
        <%= 'MOT - ' + task.delay.to_s + ' days' %>
      </div>
      <div class="col-4 pt-4">
        <%= task.description %>
      </div>
      <div class="flex-center mt-4" data-controller="edittask" data-mot-id="<%= mot.id %>" data-task-id="<%= task.id %>">
       <i class="fas fa-edit" data-action="click->edittask#editTask"></i>
      </div>
    </div>
  </div>
  <% end %>
  <% else %>
  <%= 'No task yet' %>
  <% end %>
</div>
<div >
  <div class="flex-center mt-4" data-controller="newtask" data-mot-id="<%= mot.id %>">
       <i class="fas fa-plus" data-action="click->newtask#createTask"></i>

  </div>
    <div id="wrappernewtask"></div>
</div>
<hr class="my-4">
